{
  "name": "fetch-base",
  "version": "1.0.0",
  "description": "A base class for fetch requests.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "Mike Eads",
  "license": "ISC",
  "dependencies": {
    "whatwg-fetch": "^2.0.3"
  },
  "devDependencies": {
    "typescript": "^2.7.1"
  }
}
